# Testcontainers configuration for integration tests
# Enable container reuse to keep containers running after tests complete
testcontainers.reuse.enable=true

# Disable Ryuk (resource reaper) to prevent automatic cleanup
# This is overridden by TESTCONTAINERS_RYUK_DISABLED env var in CI
ryuk.disabled=false

